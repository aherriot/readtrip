# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env (keep .env.example tracked)
.env
.env*.local
.env.development
.env.production

# drizzle
/drizzle/*.db

# playwright
/test-results/
/playwright-report/
/playwright/.cache/
# Visual baselines: only the CI (Linux) snapshots are tracked. Local runs on
# macOS/Windows generate their own platform snapshots — don't commit those.
e2e/**/*-darwin.png
e2e/**/*-win32.png

# misc
.DS_Store
*.pem
/coverage
/.claude/worktrees

# vercel
.vercel

# typescript
*.tsbuildinfo
